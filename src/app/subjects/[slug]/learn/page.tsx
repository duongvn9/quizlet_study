import{notFound,redirect}from"next/navigation";import{getSubject,subjectSlugs}from"@/data/generated/subjects.generated";export const generateStaticParams=()=>subjectSlugs.map(slug=>({slug}));export default async function Page({params,searchParams}:{params:Promise<{slug:string}>;searchParams:Promise<{restart?:string|string[]}>}){const{slug}=await params;if(!getSubject(slug))notFound();const{restart}=await searchParams;redirect(`/subjects/${slug}/study?mode=learn${restart==="1"?"&restart=1":""}`)}
