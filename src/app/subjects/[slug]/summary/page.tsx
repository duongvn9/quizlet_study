import{notFound}from"next/navigation";import{Summary}from"@/components/study/Summary";import{getSubject,subjectSlugs}from"@/data/generated/subjects.generated";export const generateStaticParams=()=>subjectSlugs.map(slug=>({slug}));export default async function Page({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const subject=getSubject(slug);if(!subject)notFound();return <Summary subject={subject}/>}
